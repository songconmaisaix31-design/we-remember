# Pull Request #1 Historical Audit

Audit date: 2026-09-02 (Asia/Shanghai)

Pull request: [#1 Add We Remember family agent and brand intro](https://github.com/songconmaisaix31-design/we-remember/pull/1)

## Decision

Do not merge or cherry-pick the pull request. All 87 commits were accounted for, and no still-useful change is missing from the current `main` branch. Merging the old branch would reintroduce conflicts and older versions of files that have since been completed, hardened, or reorganized on `main`.

The source branch must be retained. The pull request can be closed with a link to this record.

## Recorded state before closure

| Field | Value |
| --- | --- |
| Default branch | `main` |
| `main` | `52c6cd5614276f958f3ed9d58ef8eefe94c26f07` |
| PR head | `2a6e4e1dcba0562261cc7df5f3b20bf8545e1caa` |
| Merge base | `e4a81c978d41e3fb1c8377588b47cbcf6cfd3010` |
| PR state | Open, non-draft |
| Mergeability | Conflicting (`DIRTY`) |
| PR checks | No checks reported for the head branch |
| Current `main` CI | Passed at the recorded `main` SHA (Actions run `33458476101`) |

The audit fetched and inspected all remote branches and the `pre-cleanup-2026-08-31` tag before comparing history.

## Commit accounting

GitHub reports 87 commits in the pull request.

- 79 commits are direct ancestors of current `main`; their exact commits are already in the branch history.
- 7 non-ancestor commits have stable patch IDs identical to commits already on `main`:

| PR commit | Equivalent `main` commit | Change |
| --- | --- | --- |
| `3389da3` | `7307c54` | Bind acceptance projections to live state |
| `64a1687` | `2da8783` | Reject ambiguous owner member IDs |
| `56192e3` | `294092d` | Add the responsibility demo API |
| `7ea19a0` | `a0c8e2e` | Enforce the serverless request-size limit |
| `47532c6` | `229b290` | Keep private analysis in the mother view |
| `bb7a213` | `21961b1` | Limit the Vercel deployment bundle |
| `10bdb99` | `361003e` | Serve the app from the Vercel root |

- The remaining non-ancestor commit, `2a6e4e1` (`feat: add We Remember brand intro`), is represented on `main` by `fb59e47`. The three brand SVG blobs, `scripts/brand_animation_qa.mjs`, and `docs/container-motion-materials.md` are byte-identical between the old PR tip and current `main`. The live integration remains present in `app/index.html`, `app/app.js`, `app/styles.css`, and the current verification scripts. Later `main` commits, especially `49db3f`, hardened the intro and conversation motion, so the older versions must not be restored.

This accounts for all 87 commits: `79 + 7 + 1 = 87`.

## Why no extraction was made

The old PR tip is 25 commits behind the current line of development after the merge base. A tip-to-tip comparison shows that accepting the old branch would discard or regress current repository governance, CI/package metadata, the local username demo session, newer application behavior, stronger browser QA, current status documentation, and the historical-document organization.

Because every PR change is either already present verbatim, patch-equivalent, or superseded by a newer retained implementation, the minimum necessary extraction is empty.

## Reproduction commands

```powershell
git fetch origin --prune --tags
git rev-parse origin/main
git rev-parse origin/codex/we-remember-brand-intro
git merge-base origin/main origin/codex/we-remember-brand-intro
git rev-list --left-right --count origin/main...origin/codex/we-remember-brand-intro
git cherry -v origin/main origin/codex/we-remember-brand-intro
gh pr view 1 --repo songconmaisaix31-design/we-remember --json number,title,state,isDraft,headRefName,headRefOid,baseRefName,mergeable,mergeStateStatus,commits,files,statusCheckRollup
gh pr checks 1 --repo songconmaisaix31-design/we-remember
```
